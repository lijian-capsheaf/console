# -*- coding: utf-8 -*-

__capability__ = 6

def interface_vlan_del(cmd, *args, **argv):
    """
...     
_n     	del vlan interface
_f      interface vlan del [interfacename]
    """

    context = argv["context"]

    leng = len(args)

    if leng:
	from _interface_auxiliary import interface_vlan_del as del_vlan_interface
        for vlan in args:
	    exit_state = del_vlan_interface(vlan)
	    if exit_state == True:
		continue
            else:
		context.write("%s" % exit_state)
		return 
    else:
        context.write("%s:An interface that is not specified to be deleted"% cmd)
        return 
