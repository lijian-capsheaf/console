# -*- coding: utf-8 -*-

__capability__ = 6

def interface_vlan_add(cmd, *args, **argv):
    """

    """
    
    context = argv["context"]

    leng = len(args)

    if leng > 1:
	from _interface_auxiliary import interface_vlan_add as add_vlan_interface
	exit_state =add_vlan_interface(args[0], args[1])
	if exit_state == True:
	    return
	else:
	    context.write("%s" % exit_state)
	    return
    else:
	context.write("vlan interface include vlan name and vlanid")
	return 
