# -*- coding: utf-8 -*-

__capability__ = 6

def route_fromconfig(cmd, *args, **argv):
    """
    """

    context = argv["context"]

    from _route_auxiliary import route_fromconfig as fromconfig_route

    exit_state = fromconfig_arp()
    if exit_sate == True:
        return
    else:
        context.write("%s" % exit_state)

