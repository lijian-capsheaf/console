# -*- coding: utf-8 -*-

__capability__ = 6

def ipsengine(cmd, *args, **argv):
    """
...
_n      IPS
_f      ipsengine [on/off]	open and close ipsengine
        
    """
    context = argv["context"]
    state = ["on", "off"]
    
    if len(args):
        if args[0] in state:
            from _snort_ctrl import ipsengine_set as ips
	    ips(args[0])
        else:
            context.write("%s:no this parameter:%s"% (cmd, args[0]))
            return 
    else:
        context.write("%s:required parameter missing   [on/off]"% cmd)
        return
