# -*- coding: utf-8 -*-

__capability__ = 6

def sysinfo_interface(cmd, *args, **argv):
    """
    """

    context = argv["context"]

    from _interface_auxiliary import interface 
    from _prettytable import PrettyTable
    
    interface_date = interface()
    sys_inter = PrettyTable(["Interface","Status"])
    for inter in interface_date:
	sys_inter.add_row([inter[0],inter[1]])
    context.write("%s" % sys_inter)
