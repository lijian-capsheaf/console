# -*- coding: utf-8 -*-

__capability__ = 6

def virusengine(cmd, *args, **argv):
    """
...     
_n      virus defense
_f      virusengine [on/off] 	open and close virusengine
        
    """

    context = argv["context"]
    state = ["on", "off"]

    if len(args):
        if args[0] in state:
            from _snort_ctrl import virusengine_set as viru
	    viru(args[0])
        else:
            context.write("%s:no this parameter:%s"% (cmd, args[0]))
            return 
    else:
        context.write("%s:required parameter missing  [on/off]" % cmd)
        return

