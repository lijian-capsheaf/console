# -*- coding: utf-8 -*-

__capability__ = 6

def route_del(cmd, *args, **argv):
    """
    """

    context = argv["context"]

    if len(args):
        for arg in args:
            cmd = cmd + " " + arg

        import os
        os.system("%s" % cmd)
    else:
        context.write("No param")
