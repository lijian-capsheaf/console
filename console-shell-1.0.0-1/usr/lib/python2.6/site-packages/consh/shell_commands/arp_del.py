# -*- coding: utf-8 -*-

__capability__= 6

def arp_del(cmd, *args, **argv):
    """

    """
    
    context = argv["context"]

    from _arp_deal import arp_del as del_arp

    leng = len(args)
    if leng > 0:
        import socket
        try:
            ip = socket.gethostbyname(args[0])
        except:
            context.write("unknown host %s" % args[0])
	    return 
        interface = ""
        if leng > 1:
            interface = args[1]
        
        exit_state = del_arp(ip, interface)
        if exit_state == True:
            pass
        else:
            context.write("%s" % exit_state)
    else:
        context.write("%s:Unknown interface" % cmd)
        return 
