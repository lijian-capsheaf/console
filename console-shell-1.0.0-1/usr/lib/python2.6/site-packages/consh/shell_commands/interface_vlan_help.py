# -*- coding: utf-8 -*-

__capability__ = 6

def interface_vlan_help(cmd, *args, **argv):
    """
    """
    context = argv["context"]

    cmds = cmd.split()
    cmds.pop()
    cmd = " ".join(cmds)    

    helpinfo = context.get_doc(cmd)   
    if helpinfo != "":
        if helpinfo != None:
            context.write("Usage:%s" % helpinfo["format"].strip())
	    context.write("%s" % helpinfo["description"])
            return

        else:
            # #
            pass
            return 
    else:
        # #
        pass
        return
