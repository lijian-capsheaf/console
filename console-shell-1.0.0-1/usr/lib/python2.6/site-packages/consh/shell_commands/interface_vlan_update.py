# -*- coding: utf-8 -*-

__capability__ = 6

def interface_vlan_update(cmd, *args, **argv):
    """
    """

    context = argv["context"]

    leng = len(args)

    if leng > 1:
	inter = args[0]
	vlanid = args[1]
	from _interface_auxiliary import interface_vlan_update as update_vlan_interface
	update_vlan_interface(inter, vlanid)


    else:
	context.write("vlan interface include vlan name and vlanid")
